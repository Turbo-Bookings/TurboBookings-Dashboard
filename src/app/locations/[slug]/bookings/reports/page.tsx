import { TabPlaceholder } from "@/components/TabPlaceholder";

export default function ReportsPlaceholder() {
  return (
    <TabPlaceholder
      name="Reports"
      description="Daily revenue, pax, occupancy by tour, source breakdown, and CSV export for bookings + manifest. Lands in OB-6."
      phase="Phase 1"
    />
  );
}
