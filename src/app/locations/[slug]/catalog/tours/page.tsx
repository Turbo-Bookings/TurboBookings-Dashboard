import { TabPlaceholder } from "@/components/TabPlaceholder";

export default function ToursPlaceholder() {
  return (
    <TabPlaceholder
      name="Tours"
      description="The items this location sells through the booking system. Lands in Sprint B (next session) — covers the per-tour form, photos + markdown description, default duration, and the per-(item × customer-type) pricing matrix."
      phase="Phase 1"
    />
  );
}
