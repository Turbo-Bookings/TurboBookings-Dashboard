import { TabPlaceholder } from "@/components/TabPlaceholder";

export default function SchedulePlaceholder() {
  return (
    <TabPlaceholder
      name="Schedule"
      description="Recurring availability schedules (RRULE-driven) and one-off slot overrides. Lands in Sprint C + D after Tours wraps."
      phase="Phase 1"
    />
  );
}
